spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: wishlist

      username: root
      password: password
      authentication-database: admin

0springdoc:
  api-docs.path: /api-docs
  swagger-ui.path: /api-docs.html
